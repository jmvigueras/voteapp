services:
  web:
    build: .
    ports:
      - "8080:5000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - VOTE_QUESTION=Do you like this app?
    depends_on:
      - redis
    volumes:
      - ./app:/app/app
    restart: always

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
